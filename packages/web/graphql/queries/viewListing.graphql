query viewListing($id: String!) {
  viewListing(id: $id) {
    id
    make
    model
    description
    price
    latitude
    longitude
    pictureUrl
    features
    owner {
      id
      email
    }
  }
}
